# FormatJS for keep-trailing-zeros

To run the [tests for keep-trailing-zeros](packages/intl-numberformat/tests/keep-trailing-zeros.test.ts):

```
pnpm i
pnpm exec bazel test //packages/intl-numberformat:misc_tests
```
